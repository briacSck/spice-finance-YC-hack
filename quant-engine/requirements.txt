# Quant engine: stdlib only (run `python run_analysis.py` with zero installs).
# The orchestrator (agent loop + API) adds:
anthropic>=0.40
openai>=1.0
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.6
openpyxl>=3.1.0
