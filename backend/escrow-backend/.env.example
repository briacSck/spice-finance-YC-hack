# Parametric-insurance escrow backend (Base Sepolia, port 8003)
ESCROW_CHAIN_ID=84532
# ESCROW_RPC_URL=               # optional; defaults to the chain's public RPC (sepolia.base.org)
WALLET_PRIVATE_KEY=            # throwaway test key, funded with Base Sepolia ETH for gas

# Filled in by `python scripts/deploy_escrow.py` (the deploy proof prints them):
ESCROW_ADDRESS=
ESCROW_USDC=
